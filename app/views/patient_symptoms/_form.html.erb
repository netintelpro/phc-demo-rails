<%= form_for(@patient_symptom) do |f| %>
  <% if @patient_symptom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patient_symptom.errors.count, "error") %> prohibited this patient_symptom from being saved:</h2>

      <ul>
      <% @patient_symptom.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :/ %><br />
    <%= f.text_field :/ %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
