<%= form_for(@symptom) do |f| %>
  <% if @symptom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@symptom.errors.count, "error") %> prohibited this symptom from being saved:</h2>

      <ul>
      <% @symptom.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label : question %><br />
    <%= f.text_area : question %><br />
    <%= f.label :system %><br/>
    <%= select_tag(:system_id,options_from_collection_for_select(System.all, :id, :name)) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
